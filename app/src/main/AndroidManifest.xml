<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieApp"
        tools:targetApi="31">
        <activity
            android:name=".detailedviewscreen"
            android:exported="false" />
        <activity
            android:name=".Addmovieactivity"
            android:exported="false" /> <!-- SplashActivity set as the launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Other activities in your application -->
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!--
                android:exported is set to "false" because this activity is not meant
                to be launched by other apps. It will be launched by SplashActivity.
            -->
        </activity>
    </application>

</manifest>